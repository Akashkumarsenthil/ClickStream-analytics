# ============================================================
# ClickStream Analytics — Dashboard Dependencies
# ============================================================

# Core Dashboard
streamlit==1.35.0
plotly==5.22.0
pandas>=2.0
numpy>=1.24

# Data & Connectivity
requests>=2.31
firebase-admin==6.5.0

# (Note: PySpark, MLflow, and FastAPI are moved to requirements-dev.txt 
#  to avoid deployment conflicts on Streamlit Cloud)
