name: clickstream-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pandas
  - plotly
  - requests
  - pip
  - pip:
      - streamlit==1.35.0
      - boto3>=1.34.0
      - s3fs>=2024.3.0
